"""Learning from what actually happened: label every decision with its real outcome, remember which setups worked, and
report the mistakes. See outcomes.py (the labels), setups.py (the memory that feeds the agent), mistakes.py (the review)."""
